# AI 必读：项目双语开发规则

本文件适用于整个项目。AI 助手在分析或修改项目代码前，必须先阅读本文件、[双语维护说明](docs/bilingual.md) 和 [翻译工具](src/locales/translate.js)。涉及语言配置、内置分类或 AI 分析时，还必须阅读对应实现，不能仅凭本文推测行为。

## 必须遵守的原则

系统支持简体华文（`cn`）与英文（`en`），首次使用默认华文，并记住浏览器的语言选择。新增或修改功能时必须同时维护两种语言。

### `tr(text, values)`：翻译系统文字

- 新增界面文案使用 `tr()`，并在 `src/locales/messages.json` 中同时补齐 `cn` 和 `en`。
- 原始系统文字是稳定的翻译键。`tr()` 使用 `ui` 命名空间，句点和冒号按普通字符处理。
- 动态内容使用插值，两种语言的参数名称必须一致。不要用字符串拼接构造待翻译的句子。
- 找不到翻译时会显示原文；这只是回退机制，不能视为已经完成双语支持。
- 姓名、电话号码、用户输入、个案摘要、记录正文、已有 AI 分析等保留原文，不能传入 `tr()` 自动翻译。

```jsx
tr('Save Case');
tr('{{p0}} cases', { p0: 5 });
```

### `useUiLanguage()`：监听语言切换

- 在使用翻译的 React 组件中调用 `useUiLanguage()`，确保切换语言后重新渲染；已有等效的 `useLocales()` 或 `useTranslation()` 订阅时无需重复调用。
- 返回值为 `cn` 或 `en`。Hook 必须遵守 React 规则，不能放在条件分支、循环或普通工具函数中。
- `tr()` 本身不会订阅语言变化。缓存已翻译结果时，必须考虑语言依赖。
- 不要为了切换语言而刷新页面、按语言设置整个页面的 React `key`，或重置表单、筛选条件和操作状态。
- 不要把语言加入与显示无关的表单默认值依赖，避免触发 `reset()`。

```jsx
function SaveButton() {
  useUiLanguage();
  return <button>{tr('Save Case')}</button>;
}
```

### `currentLocale()`：地区格式

- 华文返回 `zh-CN`，英文返回 `en-MY`，用于日期、时间和数字的显示格式。
- 此函数不会订阅语言变化，也不会改变时区；组件仍需订阅语言。
- 不要为了翻译界面改变日期的业务含义、时区或金额币种。

### 数据值与 AI 分析

- 内置分类、辅导类型和状态仅在显示时使用 `domainLabel()`；数据库值、请求参数、筛选比较、缓存标识和记录标识必须保持稳定。
- `uiMessage()` 仅用于系统通知与验证错误，不用于用户记录正文。
- 新 AI 分析请求携带所选 `language`，语言必须纳入分析缓存范围。切换只读取缓存，不自动生成。
- 旧分析保留原文并标示过期；取消或忽略旧语言请求的迟到结果，不能覆盖当前结果。
- AI 输出的 JSON 字段、枚举和来源引用不翻译。

## 修改后的检查

- 文案或翻译调用改动：执行 `npm run test:i18n`。
- AI 请求、缓存或分析状态改动：另外执行 `npm run test:ai`。
- 应用代码改动：执行 `npm run build`。
- 按改动范围验证双向切换、表单与筛选状态保留，以及日期、分页、提示和弹窗的语言。
- 检查桌面与手机布局中的文字溢出。环境不能执行的检查必须如实注明，不能声称已经验证。
- 仅修改说明文件时，检查文档内容与链接即可，无需运行应用测试或构建。
